# Copy to .env and fill in. .env is gitignored — never commit real keys.
# NOTE: the Yandex keys below were previously hardcoded in docker-compose.yml
# and are considered compromised — rotate them in Yandex Cloud before use.

# --- Yandex Cloud (legacy yandex_v11 engine) ---
YC_FOLDER_ID=
YC_AI_API_KEY=
YANDEX_VECTOR_STORE_ID=
YC_SPEECHKIT_API_KEY=

# --- Backend admin ---
ADMIN_API_KEY=change-me-123
