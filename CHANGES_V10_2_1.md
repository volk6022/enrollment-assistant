# Исправления v10.2.1

- Бэкенд больше не инициализирует RAG во время импорта; ленивая инициализация с корректным резервным вариантом
- Конечная точка проверки работоспособности бэкенда может запускаться даже при недоступности модели встраивания
- Голосовой шлюз повторяет попытку запуска бэкенда и ожидает проверки работоспособности бэкенда в compose
- Демонстрационная версия в браузере больше не запускает вызов автоматически при загрузке страницы
- Исправлен запрос Yandex TTS v1: по умолчанию отсутствует folderId для ключа API сервисной учетной записи, используется emotion вместо role, sampleRate только для lpcm
- TTS повторяет попытку без emotion, если выбранный голос его отклоняет
- Формат TTS в демонстрационной версии браузера по умолчанию изменен на mp3